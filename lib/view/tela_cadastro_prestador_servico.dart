import 'package:flutter/material.dart';

class TelaCadastroPrestadorServico extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Serviço"),
      ),
      body: Container(color: Colors.white,),
    );
  }
}
